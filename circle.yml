machine:
  environment:
    CIRCLECI_TOKEN: test
    REDIS_URL: redis://localhost:6379/
    SECRET_TOKEN: secretsecret
  services:
    - redis

deployment:
  production:
    branch: master
    heroku:
      appname: circle-reaper
