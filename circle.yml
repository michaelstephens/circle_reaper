machine:
  environment:
    CIRCLECI_TOKEN: test
    GITHUB_TOKEN: test
    RACK_ONLY_MODE: false
  services:
    - redis

deployment:
  production:
    branch: master
    heroku:
      appname: circle-reaper
