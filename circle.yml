machine:
  environment:
    CIRCLECI_TOKEN: test
    GITHUB_TOKEN: test
    REDIS_URL: redis://localhost:6379/
    RACK_ONLY_MODE: false

deployment:
  production:
    branch: master
    heroku:
      appname: circle-reaper
