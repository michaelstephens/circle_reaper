machine:
  environment:
    CIRCLECI_TOKEN: test
    GITHUB_TOKEN: test
    RACK_ONLY_MODE: false
    REDIS_URL: redis://localhost:6379/
  services:
    - redis

deployment:
  production:
    branch: master
    heroku:
      appname: circle-reaper
